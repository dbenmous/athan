import 'package:flutter/material.dart';

class PrayerTimesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Prayer Times Calculation'),
      ),
      body: Center(
        child: Text('Prayer Times Calculation Page'),
      ),
    );
  }
}
